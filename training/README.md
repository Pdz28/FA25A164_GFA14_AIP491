# Training Module - SimplifiedHệ thống training đơn giản, tất cả trong 1 file duy nhất.## 📁 Cấu trúc```training/├── train.py          # Script training chính (all-in-one: 300 dòng)└── README.md         # File này```## 🚀 Sử dụng### Chạy training```bashpython training/train.py```### Chỉnh configMở `training/train.py` và sửa class `Config`:```pythonclass Config:    # Data paths    train_dir = "/path/to/train"    val_dir = "/path/to/val"        # Hyperparameters    batch_size = 32    epochs = 101    classifier_lr = 2e-4    # ...```## ✨ Tính năng- ✅ **DualBranchDataset**: Dataset cho CNN + ViT (tích hợp trong train.py)- ✅ **LoRA**: Efficient fine-tuning cho Swin- ✅ **Progressive Unfreezing**: Mở khóa EfficientNet ở epoch 8- ✅ **Early Stopping**: Dừng khi không cải thiện (40 epochs)- ✅ **Checkpoint**: Lưu best model theo val_loss- ✅ **Data Augmentation**: Flip, Rotation, ColorJitter## 📊 Output```Epoch 10/101  Train: Loss=0.3245 | Acc=85.67%  Val:   Loss=0.3891 | Acc=82.34%  🎉 Best model saved!```Checkpoint lưu tại: `checkpoints/best_model.pth`## 🔧 Tùy chỉnh### Thay đổi data path```pythonconfig.train_dir = "/your/train/path"config.val_dir = "/your/val/path"```### Thay đổi hyperparameters```pythonconfig.batch_size = 64config.epochs = 150config.classifier_lr = 3e-4```### Thay đổi LoRA config```pythonconfig.lora_r = 32  # Tăng rankconfig.lora_alpha = 64```## 📦 So sánh với phiên bản phức tạp| Aspect | Complex (17 files) | Simple (1 file) ||--------|-------------------|-----------------|| **Files** | 17 files, 7 folders | 1 file || **Lines** | ~3000 LOC | ~300 LOC || **Learning curve** | Steep | Flat || **Customization** | Callbacks, configs | Direct edit || **Maintainability** | High (modular) | Medium (monolithic) || **Best for** | Production | Learning/Quick experiments |---**Simple & Clean! 🎯**